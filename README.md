# birthday-for-mansingh-
birthday website for my big brother 
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Happy Birthday Mansingh</title>

    <style>
        body {
            margin: 0;
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #4e0eff, #9d4edd);
            color: white;
            text-align: center;
            overflow-x: hidden;
        }

        @keyframes float {
            0% { transform: translateY(0px); }
            50% { transform: translateY(-12px); }
            100% { transform: translateY(0px); }
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        h1 {
            font-size: 3rem;
            margin-top: 30px;
            animation: fadeIn 2s ease-in-out;
        }

        h2 {
            font-weight: 300;
            animation: fadeIn 2s ease-in-out 0.5s;
        }

        .photo-box {
            width: 260px;
            height: 360px;
            border-radius: 20px;
            margin: 30px auto;
            overflow: hidden;
            box-shadow: 0px 0px 25px rgba(255,
